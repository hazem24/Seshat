$(document).ready(function(){
        //Create Tweet Modal.
    if($("createTweet").length === 0){

    }    
        $.sweetModal({
            title: 'HTML Content',
            content: '',
            theme: $.sweetModal.THEME_DARK
        });

        /**
         * 
         */
        
});